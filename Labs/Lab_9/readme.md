# Running Node.js with nginx load balancer

1. run `docker compose up --build`

## Ex

1. add another instance to the load balancer
2. add new service in the docker compose based on the node_api
3. add relevant configuration in nginx
